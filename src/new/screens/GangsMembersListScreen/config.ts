import { GAME } from '../../../config/game';
import { SECTION_BAR_CONFIG } from '../../components/SectionBar/SectionBar.const';

export const config = {
    MEMBERS_LIST_HEIGHT: GAME.WINDOW_HEIGHT - SECTION_BAR_CONFIG.HEIGHT,
};