export const texturePathsMap4000 = [];

const numRows = 29;
const numCols = 29;

for (let row = 1; row <= numRows; row++) {
    for (let col = 1; col <= numCols; col++) {
        texturePathsMap4000.push(`map/4000/${row}/${col}`);
    }
}