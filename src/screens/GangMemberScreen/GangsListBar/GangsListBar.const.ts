import { GAME } from '../../../config/game';
import { GAME_COLORS } from '../../../config/styles';

export const CONFIG = {
    WIDTH: 500,
    HEIGHT: 100,
    SHARP_OFFSET: 50,
    HORIZONTAL_PADDING: 50,
    X: (GAME.WINDOW_WIDTH / 2) - (500 / 2),
    Y: 1,
    BACKGROUND: GAME_COLORS.black,
    BORDER_WIDTH: 3,
    BORDER_COLOR: GAME_COLORS.red1,
    // NAV_BTN_SIZE: 30
};