import { CONFIG as INVENTORY_CONFIG } from '../Inventory/Inventory.const';
import { GAME_COLORS } from '../../../config/styles';

export const CONFIG = {
    WIDTH: 40,
    HEIGHT: INVENTORY_CONFIG.HEIGHT,
    BACKGROUND: GAME_COLORS.black2,
    BORDER_COLOR: GAME_COLORS.red1,
    BORDER_WIDTH: 2,
};